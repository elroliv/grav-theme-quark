extends@: default

form:
  fields:
    tabs:

      fields:
        blog:
          type: tab
          title: KITE_LESSON.BLUEPRINT_TITLE
          ordering@: -2
          fields:

            header.lesson_price_low_season:
              type: text
              label: KITE_LESSON.LESSON_PRICE_LOW_SEASON
              placeholder:
              validate:
                type: int

            header.lesson_price_high_season:
              type: text
              label: KITE_LESSON.LESSON_PRICE_HIGH_SEASON
              placeholder:
              validate:
                type: int

        advanced:
          fields:
            overrides:
              fields:
                header.visible:
                  toggleable: false
                  default: 0
                header.routable:
                  toggleable: false
                  default: 0
        # advanced:
        #   fields:
        #     overrides:
        #       fields:
                
        #         header.visible:
        #           highlight: 0
        #           default: false
        #         header.routable:
        #           highlight: 0
        #           default: false


